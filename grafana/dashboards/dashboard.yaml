apiVersion: 1

providers:
  - name: "DatingBotDashboards"
    type: file
    updateIntervalSeconds: 30
    options:
      path: "/etc/grafana/provisioning/dashboards"
      foldersFromFilesStructure: true
